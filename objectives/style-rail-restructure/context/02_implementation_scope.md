<implementation_scope>
    <owned_surfaces>
        - `packages/docs-workbench/web/src/shell/StyleRail.tsx`: the main
          rewrite target. Today: header + segmented tabs + accordion stack.
          Becomes: header + mirrored two-pane (detail left, rail right) +
          footer. Expect to split it — e.g. StyleRail.tsx (shell/state),
          style-rail-nav.tsx (rail), style-rail-panes/*.tsx (detail panes),
          keeping the row components shared.
        - `packages/docs-workbench/web/src/theme/theme-folders.ts`:
          THEME_TOKEN_REGISTRY gains `sequence` (phase 4) plus any tokens the
          audit surfaces.
        - `packages/docs-workbench/web/src/theme/style-rail.css`: control
          styling; gains rail/two-pane rules.
        - `packages/docs-workbench/web/src/shell/App.tsx`: width class change
          site if the literal lives there after refactor; collapse handling.
        - `themes/default/components/`: add `sequence.json`; adjust files the
          audit touches.
        - `packages/docs-workbench/web/src/__tests__/style-rail.test.tsx`:
          update for new structure; add navigation + override-count tests.
        - `objectives/style-rail-restructure/`: this bundle, including the
          phase-4 output context/05_coverage_matrix.md.
    </owned_surfaces>

    <read_only_references>
        - `packages/docs-viewer/` styles: source of truth for which CSS vars
          each block consumes (phase 4 enumeration). Edits are exceptional and
          individually flagged (see 01_constraints.md).
        - `packages/docs-workbench/web/src/theme/semantic.css`: canonical
          contract vars; defaults resolve here.
        - `packages/docs-model/src/doc-schema.ts`: DOC_BLOCK_TYPES is the
          authoritative vocabulary (16 types) — read, never edit.
        - `examples/style-rail-layouts.html`: the F mockup (target geometry,
          rail grouping, dot/count treatment).
    </read_only_references>

    <generated_outputs>
        - `context/05_coverage_matrix.md`: one row per surface (16 block types
          + shell, surfaces, inline-code, editor-controls, linking + the six
          Layout areas). Columns: registry entry / panel controls / theme
          component file / consuming viewer vars / gaps found / resolution.
          Regenerated by the phase-4 audit run.
    </generated_outputs>

    <commands_and_entrypoints>
        - `make dev`: primary dev entry (Electron + HMR) for live review.
          `make app` only for static-build checks.
        - Workbench test suite (style-rail tests live in
          packages/docs-workbench/web/src/__tests__/): run after every phase;
          use the package's configured runner via its package.json script.
        - `codex exec -m gpt-5.6-sol -c model_reasoning_effort="xhigh"
          --sandbox workspace-write "<prompt>"`: all implementation edits
          (`low` only for mechanical steps like the theme JSON stub).
    </commands_and_entrypoints>

    <adjacent_surfaces_requiring_caution>
        - `docs-style-rail-settings.v1` localStorage blob: real user state on
          Ford's machines; must round-trip through normalizeSettings.
        - Per-section open-state keys
          (`docs-style-rail-section:*`): superseded by rail selection in the
          new layout — migrate or retire deliberately, don't orphan.
        - `StyleRailOverlay` (grain layer): mounted separately in App.tsx;
          restructure must not detach it.
        - Theme import/export JSON: exported themes must import identically
          across the restructure.
    </adjacent_surfaces_requiring_caution>

    <out_of_scope>
        - `docs/` corpus content and 20-implementation/40-theming docs
          (update after the work lands, as a separate docs pass).
        - docs-server, docs-cli, external-canvas packages.
        - New theme presets beyond `default`.
    </out_of_scope>
</implementation_scope>
