# Scope
New packages/docs-mcp owns discovery, MCP bridge, daemon lifecycle, direct tools, client installers, skills and snapshots. Shared guidance lives in docs-model and is reused by existing agent contexts. docs-server lock isolation and workbench proxying preserve shared authority.
