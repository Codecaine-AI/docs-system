# docs-system

When writing documentation or docs, follow the writing-style pages in the
corpus: `docs render 99-appendix/10-writing-style` (two pages — register and
structure). Block usage guidance lives with the block vocabulary:
`docs render 10-system-design/40-block-vocabulary`.
