# docs-system

When writing documentation or docs, follow the style guide in the corpus:
`docs render 99-appendix/10-style-guide` (two pages — writing style and
structure). Block usage guidance lives with the block vocabulary:
`docs render 10-system-design/40-block-vocabulary`.
