"use client";

import type { ComponentManifest } from "../types";

export const manifest: ComponentManifest = {
  name: "mermaid",
  ownedTypes: ["mermaid"],
  description: "Mermaid diagram: diagram source with live render.",
};
