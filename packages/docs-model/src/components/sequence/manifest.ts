"use client";

import type { ComponentManifest } from "../types";

export const manifest: ComponentManifest = {
  name: "sequence",
  ownedTypes: ["sequence"],
  description:
    "Sequence diagram. The block holds a reference; the sequence diagram lives in its own system.",
};
