# Guided review tour: block-architecture restructure (P1–P4)

Walk me through the completed four-phase block-architecture restructure of this repo as an interactive, station-by-station review tour. All work is committed locally and unpushed; I want to inspect, question, and optionally edit each area, then decide what to push.

## State you are starting from (verify, don't trust prose)

- Branch stack, each phase independently revertable:
  `main` → `p1-component-bundles` (5 commits) → `p2-discovery-v2` (5) → `p3-canvas-lift` (3) → `p4-viewer-mirror` (4, the tip, currently checked out).
- The `external/canvas` submodule is on branch `agent-schema` (2 commits: `92fc4e2`, `5163897`); docs-system's submodule pointer references it, so that branch must land in the canvas repo before the stack is pushed.
- Design + plan source of truth: `BLOCK-ARCHITECTURE.md` (decisions D1–D10) and `BLOCK-ARCHITECTURE-PLAN.md` (§0 interview decisions A1–A4, §9 flagged proposals) at repo root.
- `make check` is green (835 tests). The canvas package suite is green except ONE pre-existing cwd-dependent test ("validates the docs sidecar JSON files…") that predates this work.
- Orient first with: `git log --oneline main..p4-viewer-mirror` and `git -C external/canvas log --oneline -3`, then present a one-screen orientation summary and start Station 1.

## Protocol (binding)

- ONE station at a time. At each station:
  1. Re-derive "what changed and why" from the ACTUAL code/diffs (read fresh — this prompt's descriptions may be stale), compactly.
  2. Show the load-bearing code or diff excerpts (not full file dumps).
  3. Call out any DECISION items listed for that station — those are things I deliberately need to bless.
  4. STOP and wait. I reply with `approve`, a question, `edit: <request>`, `skip`, or `station <n>` to jump.
- Edits: per my global rules, ALL file edits go through `codex exec` (`-m gpt-5.6-sol`, explicit `-c model_reasoning_effort="low|xhigh"`, `--sandbox workspace-write`) — you orchestrate, Codex edits, git stays on your side. Prefer small focused codex calls over one large one (large single xhigh calls have stalled repeatedly in this environment; verify a codex session starts within ~2 minutes).
- Apply approved edits as NEW commits on `p4-viewer-mirror` unless I explicitly ask you to rewrite a phase branch — rewriting means rebasing the whole stack; warn me before doing that.
- After any edit: run `make check`; if docs-viewer source changed, run `make spa` first (stale SPA cache causes docs-cli/workbench test timeouts). Never run bare `bun test`.
- Useful per-phase diffs: `git diff main...p1-component-bundles`, `git diff p1-component-bundles...p2-discovery-v2`, `git diff p2-discovery-v2...p3-canvas-lift`, `git diff p3-canvas-lift...p4-viewer-mirror`; submodule: `git -C external/canvas show <sha>`.

## Stations

1. **Wire bytes + corpus.** The only `docs/` changes in the whole project: commit `6afc14a` normalized 6 `doc.json` files (5 corpus + the fixture) to canonical serializer output — semantically identical (verify one with `jq -S` if you like), forced by the plan's corpus re-serialize golden; plus P2/P3-era self-docs CONTENT updates (block-vocabulary, mutation-model, data-model, package-map) with their projection goldens regenerated. DECISION: bless the normalization (the alternative was weakening the golden to idempotence).
2. **The component contract** (P1 wave 0). `packages/docs-model/src/components/{types,define,checks,projection-utils}.ts`: `ComponentBundle`/`ComponentAction`/`BlockStateDefinition` shapes, `checkParams` + `schemaIssues` (TypeBox union-flatten + dedupe, `$.params.*` path mapping), pure `collectRegistryIssues` boot checks, and the golden suite under `src/__tests__/goldens/` (projection + corpus re-serialize, kept forever).
3. **The seven bundles** (P1 wave 1). `components/<name>/` = manifest, CLOSED TypeBox state schemas, one action file per verb (schema owns shape, apply keeps domain checks with verbatim messages), agent-view projection. Worth spotlighting: file-tree (the validation-split pattern), rich-text (8 types, zero actions by D7), mermaid (`caption` + `diagramType` added via the drift rule — viewer reads them). DECISIONS: `heading.level` optional / `image.src` required (plan §9.2 — corpus evidence supports both); action PARAMS schemas are OPEN (stray params ignored, matching legacy apply behavior — state schemas stay closed per A2).
4. **Kernel + compat** (P1 wave 2). `doc-ops.ts` blockAction dispatch (registry resolve → `checkParams` BEFORE apply → forward refusal → unchanged updateBlock funnel), `project-markdown.ts` reduced to the walker delegating to bundle agent-views, `components/compat.ts` legacy folds (`BLOCK_ACTIONS` stays exactly 13), `block-actions.ts` (954 lines) deleted, the monolith test running against compat with only import-line changes.
5. **Discovery v2** (P2). `src/discovery.ts` — pure fold to `{ schemaVersion: 2, ops, components }`, TypeBox schemas served verbatim; `routes.ts` reduced to `buildBlocksDiscovery()`; v1 shape, `BLOCK_TYPE_CATEGORY`, and param-spec derivation retired. DECISION: the leaf field names are the plan §4.1 proposal (§9.3 sign-off).
6. **Strict writes / tolerant reads / undo exemption** (P2). `components/validate.ts` (TypeCompiler, compiled once), insert/update refusals at `$.op.props.<key>` with no half-apply, `validateBlockProps` deleted from the read path, split/merge exempt, fixture cleanups in doc-ops.contract + workbench tests (stray props were fixture conveniences). DECISIONS: undo replay runs with `validateProps: false` (kernel option; ONLY caller is `undo_patch`) — this fixed a review blocker where an accepted write became non-undoable on legacy-prop docs (D6 adjudication); a text-only updateBlock on a block whose EXISTING props are nonconforming refuses (plan-letter choice, pinned by a named test).
7. **Canvas agent schema** (P3, submodule). `external/canvas/packages/canvas/src/agent-schema.ts`: first runtime definition of the five `CanvasAgentPatchOperation` variants — schemas mirror the hand validators' tolerance (validator-defaulted fields optional: annotation intent/status/createdBy, connection style/arrow), one-way Static<> guards where deliberately looser than the TS type, descriptor/union exhaustiveness guard, `"./agent-schema"` leaf export. NOTE: the canvas repo's standalone `bun.lock` lacks the TypeBox entry (regenerates on its next standalone install — deliberately not nested-installed from this workspace).
8. **The lift + forward routing** (P3). `components/canvas/actions/lift.ts` (`canvas.<op>` keys, `Type.Omit(envelope, ["type"])`, `forward: { authority: "canvas" }` — 18 actions in discovery, legacy folds still 13); `/api/ops` interception → `store.forwardCanvasAction`: forward-travels-alone batch rule (400 `$.ops`), `expected_hash` = doc hash / `expected_canvas_hash` = sidecar staleness, doc path lock held across resolve→apply (TOCTOU fix), realpath symlink confinement on sidecar writes, `src ?? canvasId` resolution, response `{ canvas, canvas_hash, patch_id }`, snapshot undo unchanged. Security regression tests: params-`type` dispatch-redirect and symlink escape. DECISIONS: batch rule + `expected_canvas_hash` (plan §9.6 — I approved verbally, confirm on sight); `canvasId`-only blocks get 400 (central canvas references not routable until a central authority exists server-side).
9. **Viewer mirror** (P4). `docs-viewer/src/components/` 1:1 with the model roster (callout+video inside rich-text/), descriptors + prop guards extracted from `block-registry.ts` which is now a fold with a `DOC_BLOCK_TYPES` coverage assert, per-component `editor-nodes.ts` with `schema.ts` assembling `TEXT_BLOCK_NODES`/`ATOM_BLOCK_NODES` in pinned order, `component-mirror.test.ts` + `editor-nodes-sync.test.ts`, `describeDocBlocksForAgent` deleted. Editor suites (convert/DocEditor/SlashMenu/input-rules/keymap) passed byte-untouched — the PM-schema-didn't-shift proof.
10. **Process, residuals, and shipping.** Each phase got an independent Codex review; blockers found and fixed: P2 exact-inverse/undo regression, P3 params-`type` dispatch redirect + symlink docsRoot escape (plus P1 stray-param drift and issue-array duplication). Residuals: the pre-existing canvas cwd-dependent test; canvas standalone lockfile note. Shipping order: land the canvas repo's `agent-schema` branch FIRST, then push/merge the stack in phase order (or flatten — my call). Close by listing anything I deferred at earlier stations.

Begin: run the orientation commands, give me the one-screen summary, then present Station 1.
